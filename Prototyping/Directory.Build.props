<Project>
  <PropertyGroup>
    <!-- Explicitly turn off Veldrid packaging for all projects in this subtree -->
    <PackageVeldrid>false</PackageVeldrid>
    <NoPackageVeldrid>true</NoPackageVeldrid>
    <SkipVeldrid>true</SkipVeldrid>
  </PropertyGroup>
</Project>