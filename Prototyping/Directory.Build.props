<Project>
  <PropertyGroup>
    <!-- Setting this to true for projects under Prototyping disables the guarded bits in the root targets -->
    <PackageVeldrid>false</PackageVeldrid>
  </PropertyGroup>
</Project>
